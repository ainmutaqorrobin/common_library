interface Color {
  red: number;
  green: number;
  blue: number;
}

const testpublishcommand: Color = {
  red: 255,
  green: 0,
  blue: 0,
};

console.log(testpublishcommand);

export default testpublishcommand;
